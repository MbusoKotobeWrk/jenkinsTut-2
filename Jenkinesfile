pipeline 
{
  agent any
  stages 
  {
      stage('build')
      {
        steps
        {
          sh 'echo "Welcome"'
          sh 
          '''
              echo "The multiline shell steps are working fine!!!"  
          '''
        }
      }
  }
}
